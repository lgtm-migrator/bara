import * as bara from '@bara/core';

const {
  register,
  useStream,
  useTrigger,
  useEvent,
  useCondition,
  useAction,
} = bara;

export {register, useStream, useTrigger, useEvent, useCondition, useAction};

export default bara;
